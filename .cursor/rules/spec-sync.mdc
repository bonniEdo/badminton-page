---
description: 每次功能或流程調整時，必須同步檢查並更新規格書
alwaysApply: true
---

# 規格書同步規則

每次進行任何程式調整（功能、路由、流程、API 介面、文案行為），都必須執行下列檢查：

1. 先比對實作與 `規格書.txt` 是否一致。
2. 若實作已變更，必須同步更新 `規格書.txt` 對應段落。
3. 回覆結果時要明確說明是否已檢查規格書、是否有同步修改。

## 最低要求

- 不得只改程式不改規格（若規格已不一致）。
- 不得在交付時省略規格一致性說明。

---
description: Brutalism Cute v2 設計系統（去裝飾、重層級）— 所有 UI 必須遵循
alwaysApply: true
---

# 設計系統：Brutalism Cute v2（簡化版）

## 設計目標

1. **去除多餘裝飾**：預設不使用陰影、不使用大圓角。
2. **資訊優先**：靠字級、粗細、間距與對齊建立層級。
3. **狀態清楚**：每個互動元件必須有 `default / hover / active / disabled` 規範。
4. **一致互動**：全站按鈕、輸入框、標籤、導覽列使用同一套語意樣式。

## 色彩 Token（唯一合法色票）

| Token | 色碼 | 用途 |
|---|---|---|
| `paper` | `#F7F7F2` | 全站背景、容器底色 |
| `sage` | `#9CAF88` | 主互動色、主 CTA |
| `alert` | `#3D5A44` | 強調與高亮（非錯誤紅） |
| `ink` | `#1A1A1A` | 文字、邊線、icon、分隔線 |

補充：
- 陰影色固定 `#1A1A1A`，但 **僅限少數重點元件** 使用。
- Tailwind 建議：`bg-paper`、`text-ink`、`bg-sage`、`text-sage`、`text-alert`、`border-ink`。

## 字體與排版

- 全站字體：`Noto Sans TC` / `Source Han Sans TC`（思源黑體）。
- 內文/UI：系統無襯線或既有專案預設字體。
- 行高：
  - 標題：`leading-tight`
  - 內文：`leading-relaxed`
  - 小字註記：`leading-normal`

## 形狀與深度規則

### 圓角規範（嚴格）
- `input / select / textarea`：`rounded-none`
- `button`：`rounded-none` 或 `rounded-md`（擇一頁面內一致）
- `card / panel`：`rounded-md`
- `tag / chip / badge`：`rounded-none` 或 `rounded-sm`
- 浮動 FAB：允許 `rounded-full`（唯一例外）

### 陰影規範（嚴格）
- 預設：`shadow-none`
- 可用陰影場景（只能二選一）：
  1. 主要 CTA（單一頁最多 1~2 顆）
  2. 浮動 FAB
- 合法陰影：`shadow-[4px_4px_0_0_#1A1A1A]`
- 禁止：多層陰影、柔光陰影、blur 陰影。

## 元件分類與狀態規範

## 1) Surface 類（頁面 / 卡片 / 區塊）

### Page
```
bg-paper text-ink
```

### Card（預設）
```
bg-paper border-2 border-ink rounded-md
shadow-none
```

### Card（強調）
```
bg-paper border-2 border-ink rounded-md
shadow-[4px_4px_0_0_#1A1A1A]
```

## 2) Action 類（按鈕）

### Primary Button
```
bg-sage text-ink border-2 border-ink px-4 py-2
rounded-none shadow-none font-bold
hover -> bg-alert text-paper
active -> translate-x-[1px] translate-y-[1px]
disabled -> opacity-40 cursor-not-allowed
```

### Secondary Button
```
bg-paper text-ink border-2 border-ink px-4 py-2
rounded-none shadow-none
hover -> bg-sage/15
active -> bg-sage/25
disabled -> opacity-40
```

### Icon Button（你要求的刪除/複製類）
```
w-8 h-8 inline-flex items-center justify-center
bg-paper border-2 border-ink rounded-none shadow-none
text-ink
hover -> bg-sage/15 text-sage
active -> bg-sage/25
disabled -> opacity-40
```

## 3) Form 類（輸入）

### Input / Select / Textarea
```
bg-paper text-ink border-2 border-ink
rounded-none shadow-none
focus -> outline-none ring-0 border-ink
placeholder -> text-ink/50
disabled -> bg-paper/70 text-ink/50
```

## 4) Navigation 類（Top/Bottom Bar、Tabs）

### Bottom Bar（手機）
```
外層: fixed bottom-0 w-full bg-paper border-t-2 border-ink
內層: 不加 border、不加陰影、可用分隔線
active item -> text-sage font-bold
inactive item -> text-ink/80
```

### Segmented Control（如 日/月、公平/巔峰）
```
容器: inline-flex border-2 border-ink rounded-sm bg-paper p-1 shadow-none
active: bg-sage text-ink font-bold border-2 border-ink
inactive: bg-paper text-ink/80 border border-transparent
```

## 5) Feedback 類（Tag / Badge / Modal）

### 右上角狀態 Tag（卡片）
```
bg-sage 或 bg-paper
text-ink 或 text-paper（視底色）
border-l-2 border-b-2 border-ink
rounded-none shadow-none
```

### Modal
```
overlay: bg-ink/35
panel: bg-paper border-2 border-ink rounded-md shadow-none
```

## 響應式與間距策略

- 手機（預設）：列表容器 `px-4 py-3`，卡片內距 `p-4`。
- 平板以上：`md:px-6 md:py-6`，卡片內距 `md:p-6`。
- 列表區塊 `gap` 建議：
  - 手機：`gap-4` 或 `gap-5`
  - 桌面：`md:gap-6` 或 `md:gap-8`

## 無障礙（必須）

- 互動元件必須有 `focus-visible` 狀態（至少保留 `outline` 或 `border` 變化）。
- icon-only button 必須加 `title` 或 `aria-label`。
- 文字對比需符合可讀性（避免 `text-ink/30` 以上低對比用於主要資訊）。

## 禁止事項（強制）

1. 不使用規範外色票。
2. 不使用漸層背景。
3. 不使用大圓角（`rounded-xl` 以上）作為預設。
4. 不使用陰影作為常態（僅重點 CTA / FAB 例外）。
5. 不同頁面不可自行發明新的按鈕狀態語言（需沿用本規範）。
